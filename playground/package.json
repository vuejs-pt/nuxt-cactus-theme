{
  "name": "nuxt-cactus-theme-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "postbuild": "pagefind --site .output/public",
    "preview": "npx serve .output/public"
  }
}
