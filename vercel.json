{
  "installCommand": "pnpm install",
  "buildCommand": "nuxt build playground && (pagefind --site playground/.vercel/output/static || echo 'Pagefind: no pages to index, skipping') && mkdir -p .vercel && cp -r playground/.vercel/output .vercel/",
  "framework": null
}
